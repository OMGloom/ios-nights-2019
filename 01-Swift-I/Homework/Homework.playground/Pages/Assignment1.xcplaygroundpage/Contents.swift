import Foundation

class Node {
    let value: Int
    let left: Node
    let right: Node
    
    init(value: Int, left: Node, right: Node) {
        self.value = value
        self.left = left
        self.right = right
    }
}

func max(in node: Node) -> Int {
    // TODO: Replace the return with your implementation
    return 0
}
